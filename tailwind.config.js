/** @type {import('tailwindcss').Config} */
module.exports = {
  purge: ['./src/**/*.{ts,html}'], // Update this line to include Ionic's file extensions
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
