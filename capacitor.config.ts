import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'humble-habit-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
